import JournalList from './JournalList'

// This component now just renders the journal list
// Individual entries are handled by JournalEntry component via routing
export default function Journal() {
  return <JournalList />
} 